// Polls the DAG for eligible_for_execution tasks
// Reads task description, context, builds additional context if needed, creates patch
// Validates patch, reviews diff, ensures quality and completion
// Create branches, PRs, view CI/CD status, merge
// Handles success/failure results